# niyama — top-level test entry (referenced by cyrius.cyml [build].test).
# For unit tests, prefer adding cases to tests/niyama.tcyr.

include "lib/syscalls.cyr"

fn main(): i64 {
    return 0;
}

var r = main();
syscall(SYS_EXIT, r);
